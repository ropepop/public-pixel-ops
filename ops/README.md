# Ops Outputs

This directory is reserved for generated operational output.

- `ops/evidence/` for deploy and runtime evidence
- `ops/reports/` for generated diagnostics and summaries

Do not commit generated files from those directories to the public starter.
