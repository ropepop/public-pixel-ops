# Ticket V2 release

User authorized implementation of the proposed V2 plan on 2026-09-06.

Keep all current Ticket features, including HDR, administration, statistics,
scheduling, account switching and the separately confirmed full reset. The user accepted the measured 40.17% reduction on 2026-09-07, waiving the
original 50% target. Reduce handwritten production code across ops and Pixel. Test deletion,
generated output, minification and moving code into shared libraries do not count.

One authoritative owner for permission/quotas, durable action settlement, phone
recognition/input, capture, relay delivery, and browser presentation. Keep exact
ticket identity, two agreeing phone observations, touch/display/input protection,
and the existing conclusively proved second registration stroke. Remove Android
hierarchy slider geometry, browser ViVi recognition, legacy execution paths,
overlapping recovery and source-shape test machinery.

The browser gets one automatic recovery attempt per failure episode, then a
reconnect control. Control-code results require exact encoded-frame identity and
presentation acknowledgement; physical execution never retries for delivery.

Acceptance: accepted 40.17% reduction; five consecutive successes per action route; all feature journeys; focused critical tests;
actual Brave cold/warm timing no worse than measured V1 (user changed the browser
acceptance target to Brave only on 2026-09-07); actual
Pixel action, result and quiet state. On 2026-09-07 the user explicitly excluded
brightness verification and the active-action physical-touch interruption check. Hands-on verification
is primary. Do not reset the live linked account merely to test full reset.

Deploy during coordinated maintenance after draining actions. Keep the existing
database and its recorded outcomes; no destructive data publication. Preserve
sessions, secrets boundaries, mirrors and user access. Rollback never restores
a database snapshot that could forget an executed action.
